/// Typically `dev`, `staging`, `prod`
///
/// Used to segment our Firebase services into different databases or storage buckets.
const environment = 'dev';

/// The title of the app
const appTitle = 'Firebase Architecture Demo';
